<p>Welcome <%= @resource.email %>!</p>
<p>You can confirm your account email through the link below:</p>
<p><%= link_to 'Confirm my account', api_user_confirmation_url(:confirmation_token => @token, token: @resource.oauth_token) %></p>